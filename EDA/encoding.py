# To  convert categorical dat from the column the column "Car_Brand" to numerical data
# Use get_dummies

# f_encode = pd.get_dummies(df, columns=[Car_Brand])

# THE categorical data is converted is converted to binary format of Numerical data

